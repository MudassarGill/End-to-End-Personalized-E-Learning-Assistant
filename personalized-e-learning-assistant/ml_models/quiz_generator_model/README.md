# Quiz Generator Model

This directory contains the saved quiz generation model artifacts.

Place your fine-tuned or downloaded model files here (e.g., `pytorch_model.bin`, `config.json`, `tokenizer.json`).
